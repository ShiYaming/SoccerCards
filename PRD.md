# SoccerCards 球星卡二级市场估价工具 — PRD

> 版本：0.2 · 更新：2026-08-16 · 状态：MVP 已可运行（本地单机）

## 1. 产品概述

### 1.1 一句话定位

一个面向足球球星卡（Topps / Panini）的**二级市场估价与行情工具**：输入一张卡，给出当前合理市场价、近期成交依据、未来 3/6/12 个月的估价区间。

### 1.2 要解决的问题

- 球星卡二级市场**价格不透明**：成交分散在 eBay、拍卖行、卡淘等平台，普通藏家很难判断一张卡"现在值多少"。
- **估价难**：价格受球员表现、大赛周期、稀缺性（平行/限量/评级）、市场热度共同影响，人工判断误差大。
- **数据分散**：卡片主数据（checklist）、成交记录、球员数据、评级人口分属不同来源，需要整合。

### 1.3 目标用户

| 用户 | 核心诉求 | MVP 对应功能 |
| --- | --- | --- |
| 藏家 | 我的卡值多少钱 | 单卡查询、当前估值、成交依据 |
| 交易者 | 现在买还是卖 | 价格趋势、未来估价区间 |
| 卡店/批量用户 | 批量估价 | API（二期） |

### 1.4 产品边界（当前不做）

- 不做交易撮合、托管、支付（合规与资金复杂度完全不同）。
- 不做全品类体育卡，专注足球细分（新秀概念、大赛驱动与美式运动不同）。
- 估值是参考价，不构成投资建议。

## 2. 设计思路

### 2.1 核心原则

1. **卡片身份（Card Identity）是地基**。估价系统的第一性问题不是"怎么算价"，而是"同一张卡如何被唯一标识"。eBay 标题是脏数据，绝不能当身份。身份 = 系列 + 卡号 + 球员 + 平行 + 限量 + 评级（+ 证书号）。
2. **数据分层、合法优先**。官方 API > 云端采集服务 > 自建爬虫 > 手动导入；灰色爬取只作过渡。
3. **估价分层、可解释**。V0 参考价（真实成交）→ V1 特征回归（模型推断）→ V2 时序预测（后续）。冷门卡靠模型、热门卡靠数据，按样本量加权混合。
4. **数据管道幂等**。重复采集/导入不产生重复数据；数据库可由数据源完整重建。
5. **诚实的置信度**。样本不足时给区间而不是硬给单一价格；方法说明向用户公开。

### 2.2 系统架构

```text
采集层                   主数据/存储                服务层              展示层
┌────────────────┐   ┌──────────────────────┐   ┌──────────────┐   ┌──────────┐
│ TCDB checklist │   │ 卡片身份（cards/sets） │   │ V0 参考价     │   │ Web 估价台│
│ Transfermarkt  │ → │ 球员（市值/年龄/位置） │ → │ V1 特征回归   │ → │ 榜单/检索 │
│ Apify eBay已售 │   │ 成交（sales）          │   │ 预测（动量外推）│   │ 详情/图表 │
│ （PSA 待接入）  │   │ 人口/评级（populations）│   │ FastAPI      │   │          │
└────────────────┘   └──────────────────────┘   └──────────────┘   └──────────┘
```

### 2.3 数据流（一条成交如何变成估值）

```text
eBay 标题 → 系列识别 → 匹配 checklist 卡片（球员名+卡号双信号）
         → 提取 评级/平行/限量 → 归一化入库（幂等去重）
         → V0 窗口参考价 + V1 模型价 → 按样本量混合 → 动量外推预测
```

## 3. 数据源与数据现状

### 3.1 数据源清单

| 数据源 | 用途 | 状态 | 说明 |
| --- | --- | --- | --- |
| TCDB | 卡片主数据（系列/checklist/插入卡） | ✅ 已接入 | 足球系列覆盖全；命名与 eBay 俗称不同，需别名映射 |
| Transfermarkt | 球员市值/年龄/位置/俱乐部 | ✅ 已接入 | 作估值特征，非价格 |
| Apify（eBay 已售 actor） | 成交价 | ✅ 已接入 | 无需 eBay 审核；$2.5/千条；Cloudflare 由平台处理 |
| eBay 官方 API | 成交价 | ⏳ 待审核 | 审核通过后做主数据线（Marketplace Insights） |
| PSA | 评级人口/证书号 | ⏳ 未接入 | 本机直连被 403，需抓包或第三方服务 |
| 130point | Best Offer 成交价 | ❌ 不可用 | Cloudflare 全面阻断 |
| 卡淘/闲鱼 | 中文渠道价格 | ⏳ 未接入 | 目标含华人市场时再接入 |
| 卡淘（Card Hobby） | 中文市场成交价 | ✅ 已接入（实验性） | `cardhobby.com.cn` 已售接口；中文标题匹配率低，作参考价 |

### 3.2 当前数据规模（2026-08-16）

| 指标 | 数值 |
| --- | --- |
| 系列 | 7（Chrome UEFA / Merlin / Prizm WC / Prizm EPL / Bundesliga + 2 插入系列） |
| 卡片 | 1,301 |
| 球员 | 826（18 位补全市值） |
| 真实成交 | 249 笔（eBay 240 + 卡淘 9，约 $0.7 额度） |

## 4. 主要功能（MVP 已实现）

### 4.1 数据与身份

- **TCDB checklist 采集**：`tcdb sets / checklist / inserts`，支持分页、插入卡系列、slug 自动转换、503 限流退避。
- **系列识别层**：从标题识别所属系列，插入卡名（如 Wonderkids）优先级高于产品名；中英检索别名。
- **标题匹配器**：球员名（重音归一化）+ 卡号双信号匹配，提取评级（PSA/BGS/SGC/CGC）、限量（`#/399`、`135/150`）、平行关键词。
- **玩家合并与幂等**：TCDB 与 Transfermarkt 球员按名字合并（有市值优先）；成交唯一索引 + INSERT OR IGNORE。

### 4.2 估价

- **V0 参考价**：按窗口（30/90/365 天）取中位数，IQR 清洗异常价，样本不足自动扩窗，输出区间。
- **V1 特征回归**：21 维特征（评级档位、平行稀有度、限量印量、市值、年龄、位置、流动性、标题提示等），Ridge（最优）+ RandomForest，按卡片分组 5 折交叉验证。
- **混合估价**：`predict` 输出 V0 / V1 模型 / 混合三档，样本越多越信 V0（`w = min(0.8, n/8)`）；冷门卡纯模型推断。
- **未来预测**：当前估值 × 近 180 天成交动量外推（日漂移 ±0.2% 上限、按样本量衰减、置信带随样本收窄），输出 3/6/12 个月区间。

### 4.3 Web 估价台

- 主流卡片榜单（成交热度 Top 12）：均价/中位价/当前估值/3·6·12 个月预测。
- 检索：中英文球员名、系列、卡号、平行，系列过滤。
- 卡片详情：市值、成交散点图、预测曲线（含置信区间带）、最近成交明细、方法说明。
- 纯静态前端（无 CDN），本地可跑；FastAPI 提供 4 个 JSON API。

### 4.4 CLI

`init-db / player / ebay / apify / ebay-scrape / import-sales / tcdb / match / match-file / estimate / train / predict / serve`

## 5. 不足与风险（诚实评估）

### 5.1 模型精度

- **CV MAPE ~80%**（Ridge），离产品目标（<25%）差距大。
- **瓶颈不是模型，是标签质量**：评级标签覆盖率仅 22%，平行标签覆盖率仅 19%；大量插入卡/平行成交（标题未标注）被归到基卡身份，污染价格标签。
- 样本量小（240 笔），按卡片分组后每卡样本更少，模型难以学到稳定规律。

### 5.2 数据质量

- 基卡身份混入插入卡/平行成交 → 榜单"均价"与"当前估值"可能显著背离（例：梅西 Prizm WC 基卡均价 $401 vs 中位价 $19）。
- 评级信息常缺失：很多标题只有证书号后缀（`&07231`），评级需证书号反查 PSA。
- 搜索覆盖仅限已入库系列（2026 NSCC 等未入库搜不到）。

### 5.3 预测方法

- 当前预测是**简单动量外推**：未建模大赛周期（世界杯/欧冠/转会窗）、球员身价趋势、搜索热度，长周期（1 年）置信带极宽，参考价值有限。

### 5.4 工程与合规

- SQLite 单机存储：无并发写入能力，不适合多用户在线服务。
- 无定时调度：数据靠手动命令采集，未做增量刷新。
- 合规风险：Apify actor 采集 eBay 属 ToS 灰色地带；第三方数据（Card Ladder 等）有转售限制；卡图有版权。
- 无用户体系、无组合跟踪、无价格提醒。

## 6. 后续优化建议（按优先级）

### P0 — 数据质量（决定估值精度的最大杠杆）

1. **PSA 证书号评级归因**：采集证书号 → 反查 PSA 评级/人口 → 成交按评级分桶。预期直接改善模型与榜单偏差。
2. **完整平行/插入卡系列入库**：Prizm WC / EPL 等系列的平行与插入卡在 TCDB 均为独立 set，`tcdb inserts --ingest` 即可批量接入，消除"插入卡当基卡"。
3. **扩大采样**：覆盖更多球员/系列查询（预算可控），接入 eBay 官方 API（审核通过后）补历史成交，积累跨周期数据。

### P1 — 模型与预测

4. **特征增强**：FBref 表现数据（进球/助攻/出场）、Google Trends 搜索热度、大赛日历因子（距世界杯/欧冠决赛天数）、人口报告稀缺性。
5. **时序预测升级**：Prophet / 贝叶斯结构时间序列替代简单外推；分位数回归直接输出区间；对热门卡单独建模，冷门卡走相似卡定价（k-NN 或聚类）。
6. **评级分模型**：按评级（PSA 10 / PSA 9 / 裸卡）训练独立模型或加入交互特征。

### P2 — 产品功能

7. 组合跟踪（收藏夹 + 总估值）、价格提醒（涨跌阈值）、历史回测（验证估值误差）。
8. 批量估价（CSV 上传 / API），面向卡店。
9. 中文渠道接入（卡淘 ✅ 已实验接入 / 闲鱼）作为华人市场参考；卡淘标题清洗（多卡打包、杂牌产品过滤）后可用于模型。

### P3 — 工程化

10. Postgres 迁移 + 定时采集调度（GitHub Actions / cron）+ 失败告警。
11. Docker 化部署、数据版本化（raw 快照）、监控面板。
12. 数据合规评估：来源标注、ToS 审查、商用前取得数据许可。

## 7. 成功指标

| 指标 | 当前 | 目标 |
| --- | --- | --- |
| 估值误差（对近 60 天成交的中位误差率） | ~80% | <25%（P0 数据质量后先冲 <50%） |
| 卡片覆盖率（目标系列 Top 球员可查询） | 7 系列 | 10+ 旗舰系列 |
| 数据新鲜度（成交滞后） | 手动 | <48h |
| 评级标签覆盖率 | 22% | >70%（PSA 归因后） |

## 8. 里程碑

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M0 | 预分析 + 数据源验证 | ✅ |
| M1 | 数据管道（TM/TCDB/Apify）+ 卡片身份 + V0 | ✅ |
| M2 | V1 特征回归 + 混合估价 | ✅ |
| M3 | Web 估价台（榜单/检索/详情/预测） | ✅ |
| M4 | 评级归因 + 完整平行/插入卡入库 + 精度提升 | ⏳ |
| M5 | 时序预测升级 + 组合跟踪 | ⏳ |
| M6 | 工程化部署 + 商业化评估 | ⏳ |

## 9. 附录：关键决策记录

- **成交价数据源**：eBay 官方 API 需审核 → 先用 Apify actor `caffein.dev/ebay-sold-listings`（只返回真实已售，$2.5/千条）；130point 实测被 Cloudflare 阻断，弃用。
- **主数据源**：TCDB（足球覆盖全）；命名差异用 `SET_ALIASES` 映射（如 eBay "2023 Topps Chrome UEFA" = TCDB "2023-24 Topps Chrome UEFA Club Competitions"）。
- **估价方法**：V0 用中位数而非均值（抗污染）；V1 用对数价格回归（跨量级稳定）；混合权重按样本量。
- **预测边界**：动量外推明确标注为 MVP 方案，长周期置信带应视为"区间示意"而非确定性预测。
