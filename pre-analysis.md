# 球星卡二级市场估价工具 — 预分析

> 日期：2026-08-16 · 状态：草案（待确认产品形态后细化）

## 1. 产品定位

核心问题：**对一张具体的球星卡，给出“当前合理市场价”，并让用户能看懂价格为什么是这样。**

建议的定位：

- 估值/查询工具优先，交易撮合、托管、支付留到以后（资金与合规复杂度完全不同）。
- 专注足球细分市场，而不是全品类体育卡。足球卡的“新秀卡”概念、大赛驱动（世界杯/欧冠）与球员年龄周期，都和棒球/篮球卡逻辑不同，专注反而容易做出差异化。
- 中文/华人用户友好：UI、卡名、系列名做本地化，同时可以整合卡淘、闲鱼等中文渠道的价格作为参考。

目标用户分三类，功能优先级不同：

| 用户 | 核心诉求 | 对应功能 |
| --- | --- | --- |
| 藏家 | 我的卡值多少钱 | 单卡查询、组合估值、评级卡估值 |
| 交易者 | 现在买还是卖 | 价格趋势、动量、低估/高估提示 |
| 卡店/评级商 | 批量估价 | API、批量导入（二期） |

## 2. 核心数据模型：卡片身份（Card Identity）是地基

估价系统的第一性问题不是“怎么算价”，而是**“同一张卡如何被唯一标识”**。eBay 标题是脏数据，绝不能拿它当卡片身份。

建议的卡片身份模型：

```text
Card
├── Set（系列）：2023 Topps Chrome UEFA Champions League
├── Card #（卡号）+ 球员
├── Parallel（平行）：Base / Refractor / Gold /99 / 1/1 ...
├── 限量编号（Serial Number）
├── Variant：Base / Auto / Patch / Memorabilia
├── 评级（Grade）：PSA 10 / BGS 9.5 / SGC 10 / 未评级
└── 证书号（Cert #，评级卡的唯一凭证）
```

一次成交记录（Sale）：

```text
Sale
├── card_id（外键，规范后的卡片身份）
├── 时间 / 平台（eBay / 130point / 拍卖行 / 卡淘 / 闲鱼）
├── 成交价（原币种）+ 换算基准币种价
├── 含运费？含税？→ 归一化字段
├── 拍卖成交 / 一口价（BIN）成交
├── 评级与否、证书号
└── 原始标题 + 图片 URL（供人工复核）
```

主数据建议以 **TCDB（The Trading Card Database）或官方 checklist** 为基准，自维护一套 `card_id`。系列、卡号、平行、球员映射都要落到这个表里。

## 3. 数据源分析

### 3.1 分层策略

| 层级 | 数据源 | 拿什么数据 | 方式与成本 | 评价 |
| --- | --- | --- | --- | --- |
| 第一层（必须） | eBay Marketplace Insights API | 售出记录（价格/时间/标题），活跃挂单可用 Browse API | 官方 API，需开发者账号与额度；成本低 | **最合法的成交价来源**。注意 legacy Finding API 已于 2025 年初退役，只能走 Marketplace Insights + Browse。成交数据需自建历史库，不能依赖 API 回溯太久 |
| 第二层（增强） | 130point.com | eBay 成交价（含 Best Offer 最终成交价，eBay 前端不显示） | 无官方 API，只能爬；ToS 灰色、有反爬 | 价格信息密度高，但**抓不到评级/平行时容易串价**，必须清洗；不建议作为唯一来源 |
| 第二层（增强） | PSA 官网 | 拍卖成交记录（Auction Prices Realized）+ 人口报告（各评级数量） | 官网公开页可爬，或经 Apify/第三方 API | 评级卡稀缺性（人口）是估值核心特征 |
| 第二层（增强） | TCDB | 系列/卡号/平行/球员 checklist | 社区维护、可抓取/有 API 项目 | 卡片身份主数据的首选，需核对足球卡覆盖度 |
| 第三层（可选付费） | Card Ladder | 号称 1 亿+ 历史成交（eBay/Goldin/Heritage/Fanatics 聚合），有 API | 付费订阅/企业合作 | 适合做**基准校验**，或直接买数据省掉自建采集。注意数据转售条款 |
| 第三层（可选付费） | Market Movers（Sports Card Investor） | 价格图表、量、趋势 | 付费 | 与 Card Ladder 二选一即可，偏美国市场 |
| 补充 | Transfermarkt | 球员市场价值、转会费、年龄、球队 | 官方无公开 API，可爬或买第三方 | **不是价格，但做估值特征非常好**：足球卡估值和球员身价强相关 |
| 补充 | FBref / API-Football | 进球、助攻、出场等表现数据 | API/爬取 | 近期表现 → 价格动量特征 |
| 中文市场补充 | 卡淘、闲鱼 | 中文渠道成交/挂单 | 爬取，合规需注意 | 如果目标用户含华人市场，这是差异化数据 |

### 3.2 推荐组合（按优先级）

1. **MVP 必做**：eBay Marketplace Insights API（合法成交价）+ TCDB/官方 checklist（卡片身份）+ Transfermarkt（球员数据）。
2. **第一轮增强**：PSA 人口报告（评级稀缺性）+ 130point 补 Best Offer 成交价。
3. **有钱/要省事再上**：Card Ladder 数据作为价格基准，用来校验自己的模型。

### 3.3 足球卡特有的数据要点

- 主流系列：Topps Chrome UEFA Champions League、Topps Merlin、Panini Prizm World Cup、Panini Prizm EPL、Topps Chrome Bundesliga、Panini Select/National Treasures/Immaculate/Flawless、Donruss、Obsidian。
- 热门球员随大赛周期变化：2026 世界杯刚过，Yamal、Endrick 这类年轻球员卡片在大赛前后波动剧烈，这正是工具要捕捉的信号。
- 足球卡“新秀卡”概念不如美式运动标准统一，需要自己定义“首年版/新秀年”规则（例如球员在该系列中的首个赛季卡）。
- 同一张卡在不同国家印刷（Topps Chrome 多国印刷），品控和需求都不同，估价时要能区分产地。

## 4. 估价方法论（从简单到进阶）

### V0：参考价（1–2 周可上线）

- 按 `card_id + 评级` 分组，取最近 30 天成交价的中位数/截尾均值。
- 交易量不足时自动扩大窗口：30 → 90 → 365 天。
- 仍无数据时，借用同系列同类平行卡的价格比例（如 Refractor 相对 Base 的常见溢价）推算。
- 输出单一价格 + 样本数 + 区间。

### V1：特征回归（MVP 建议做到这层）

用轻量模型（线性回归 / GBM）做估价，特征分三类：

```text
卡片层：限量数、人口报告数量、平行等级、评级、系列热门度、卡龄
球员层：年龄、位置、近期表现（进球/助攻/出场）、Transfermarkt 市值、国家队/俱乐部
市场层：近 30/90 天成交笔数、搜索热度、大赛事件（世界杯/欧冠/转会）
```

关键点：**冷门卡用模型推断（靠特征），热门卡用真实成交（靠数据）**，两者按样本量加权。

### V2：时序与动量（二期）

- 对成交活跃的卡做时间序列（指数平滑/Prophet），输出趋势与涨跌幅。
- 对冷门卡回退到 V1/V0，并明确标注“低流动性，估值置信度低”。

### 4.1 常见陷阱（必须处理）

- 脏数据：1 美元的垃圾价、1/1 天价、卖家改价、Best Offer 价与展示价不符。
- 同卡不同平行混价（Refractor 和 Base 完全两个市场）。
- 未评级卡与评级卡混价（PSA 10 通常贵 3–10 倍）。
- 币种、运费、税未归一化。
- 样本量少时只给单一价格，误导用户 → 必须给置信区间。

## 5. 系统架构（MVP）

```text
┌─────────────────────────────────────────────────────┐
│ 前端：Next.js（或 MVP 用 Streamlit 快速验证）         │
│ 功能：搜卡 → 估值 + 近期成交 + 趋势 + 影响因素解释     │
└──────────────────────────┬──────────────────────────┘
                           │ REST
┌──────────────────────────▼──────────────────────────┐
│ 服务层：FastAPI                                      │
│ /search  /card/{id} /price  /history  /portfolio    │
└──────────────────────────┬──────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────┐
│ 数据层：PostgreSQL                                   │
│ cards（身份主数据）｜ sales（成交明细）               │
│ populations（人口）｜ players（球员）｜ valuations     │
└──────────────────────────┬──────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────┐
│ 采集层：Python 定时任务（GitHub Actions / cron）      │
│ eBay API → 130point → PSA 人口 → Transfermarkt      │
│ 每日刷新；限速 + 退避 + 采集成功率监控                │
└─────────────────────────────────────────────────────┘
```

技术选型不是关键，关键是**身份主数据 + 成交明细库 + 定时增量采集**这三件事的 schema 从第一天就设计好。

## 6. 风险与合规

- **ToS/法律**：eBay 开发者计划条款、130point/Card Ladder 的抓取与数据转售限制、卡片图片版权。设计时就要留“数据来源 + 引用”字段，避免整库转售第三方数据。
- **采集稳定性**：爬虫会被封、API 有配额；所有采集任务要有失败重试、限速、告警。
- **汇率与税**：成交价统一归一化到基准货币（如 USD/CNY/TWD），记录原始币种。
- **免责**：估值仅供参考，不是投资建议；工具页面上应注明。

## 7. MVP 范围建议（8–12 周）

| 模块 | 内容 | 优先级 |
| --- | --- | --- |
| 卡片主数据 | 3 个旗舰系列：Topps Chrome UEFA（近 4 年）、Panini Prizm World Cup 2022/2026、Panini Prizm EPL；Top 50–100 球员 | P0 |
| 成交采集 | eBay Marketplace Insights API + 自建 sales 库 | P0 |
| 估值 | V0 参考价 + V1 特征回归，输出价格与置信区间 | P0 |
| 球员数据 | Transfermarkt 市值 + 基础表现数据 | P1 |
| 评级数据 | PSA 人口报告（覆盖主力系列） | P1 |
| 前端 | 搜索、卡片详情、价格历史、涨跌标记 | P0 |
| 中文渠道 | 卡淘/闲鱼价格作为参考 | P2 |

成功指标建议：

- 估值误差：对近 60 天真实成交，估值中位误差率 < 25%（可逐步收紧到 20%）。
- 覆盖率：Top 100 球员在主力系列中的卡片可查询率 > 90%。
- 数据新鲜度：成交数据滞后 < 48 小时。

## 8. 下一步建议

1. **先确认产品形态**：个人自用工具 / 社区免费工具 / 商用 SaaS？这决定数据源预算（Card Ladder 是否买）和合规投入。
2. **做数据 PoC（最重要）**：选 10–20 张热门卡（如 Yamal 2023 Topps Chrome UEFA Refractor、Messi/Ronaldo 世界杯卡），手动验证 eBay API、130point、PSA 人口、Transfermarkt 四条数据线的可得性、字段完整度和价格差异。这一步能直接验证“估价可不可行”。
3. **搭骨架**：建库 schema + eBay 采集 + V0 估值，先跑通一条“搜卡 → 出价”的闭环。
4. **再上模型**：V1 特征回归，用 PoC 阶段积累的成交数据训练。

---

## 附录：数据源实测结果（2026-08-16 更新）

### 无密钥替代路径

| 路径 | 实测 | 结论 |
| --- | --- | --- |
| eBay 官方 API | 需开发者密钥（审核中） | 审核通过后为主数据线 |
| eBay 公开搜索页/RSS 直连 | Akamai JS challenge，requests/curl_cffi 均 403 | 本机不可行 |
| 130point | Cloudflare 全面阻断 | 不可行 |
| PSA 网页 | 403 | 本机不可行，需抓包或第三方服务 |
| **Apify 云端采集** | ✅ 跑通：`caffein.dev/ebay-sold-listings`，30 条真实成交，单次约 $0.08 | 当前主力 |
| Playwright 本机爬取 | 代码就绪，未实测（需装浏览器） | 过渡选项 |
| 手动导入 CSV/JSON | ✅ 跑通「导入 → 估价」闭环 | 兜底 |
| TCDB 卡片主数据 | ✅ 跑通：足球系列全、checklist 可抓、命名需别名映射 | 卡片身份主数据源 |

### 真实数据验证的两个关键结论

1. **卡片身份必须先行**：实测同一搜索词（Yamal 2023 Topps Chrome UEFA Refractor）混着 7 种平行×评级组合，价格 $22–$1,462；混合估价中位数 $305、区间 $35–$925 无参考价值，按「Silver Refractor + PSA 9」分组后 2 条真实成交（$337/$411）估值为 $374。这正是第 2 节强调的 Card Identity 问题的实证。
2. **评级信息不全**：多条成交标题只有证书号后缀（如 `&07231`）没有评级字样，评级需通过证书号反查 PSA；采集层要保留 itemId/证书号字段，为后续评级归因留数据。

### 卡片主数据阶段进展（2026-08-16）

- TCDB 已接入：`tcdb sets` 列出系列、`tcdb checklist` 抓清单入库；2023-24 Topps Chrome UEFA Club Competitions（sid=434800）235 条清单已入库。
- 系列覆盖扩至 5 个旗舰系列 + 2 个插入系列（共 7 个 set、1,278 张卡）：Chrome UEFA / Merlin / Prizm World Cup / Prizm EPL / Chrome Bundesliga / Wonderkids / Pulsar Refractor。
- 系列识别层已实现：30 条真实成交 28/30 自动归到正确系列（Wonderkids/Merlin/Pulsar 均正确），2 条 2026 NSCC 正确判为未识别。
- 数据管道幂等：重复导入不产生重复成交；球员按名字跨源合并；成交去重唯一索引。
- 待解决：其余基卡平行系列（Aqua Prism / Neon Green Wave 等在 TCDB 是独立 set）按需入库；评级归因需要 PSA 证书号反查。

### V1 特征回归模型（2026-08-16）

- 真实成交 240 笔（9 个 Apify 查询），21 维特征（评级档位、市值、年龄、位置、平行稀有度、限量、流动性等），按卡片分组 5 折交叉验证。
- 结果：Ridge MAPE 79.7%（不加评级/标题特征时为 152%），特征重要性前二为评级档位与评级标签覆盖率——直接验证了"评级归因是最大数据杠杆"。
- 混合估价已上线：`predict` 输出 V0 参考价 + V1 模型价 + 样本量加权混合价；冷门卡走纯模型推断。
- 下一步：PSA 证书号评级归因 → 完整平行/插入卡系列入库 → 扩大采样；数据质量到位后同一管道可冲击 <50% MAPE。
